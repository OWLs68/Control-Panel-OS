node_modules/
dist/
*.local

# Playwright
playwright-report/
test-results/
blob-report/

# Build output of the unit tests
.tmp/

# Env — never commit real secrets. Only .env.example is tracked.
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.log
npm-debug.log*

# TS build info
*.tsbuildinfo

# Службові метадані сесії Claude. Команди в .claude/commands/ — комітяться,
# межі конкретної сесії — ні (вони дублюються в HANDOFF → ПОТОЧНИЙ СТАН).
.claude/.session-runtime.json

