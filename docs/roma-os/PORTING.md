# Що перенесено з NeverMind дослівно — і як переносити далі

> Головний урок цієї сесії. Читати **перед** тим, як торкатись будь-якого
> компонента, який у NeverMind уже є.

---

## 1. Правило, яке я порушив і через яке довелось переробляти

Перша версія поля вводу, вікна чату і таббара була **написана з нуля**:
свої розміри, свої криві, свої класи. Здалеку схоже на донора, зблизька —
спрощення. Роман це побачив одразу.

**Правило:** якщо механіка є в NeverMind — беремо **файл, розмітку і всі
числа**, а не «робимо схоже». Свої тільки:
- палітра (Graphite Amber замість NeverMind),
- іконки й підписи Roma OS,
- структурні рішення, які Роман ухвалив явно (один чат замість восьми).

Усе інше — донорське, включно з тим, що виглядає дивно. Дивне там зазвичай
і є фіксом.

---

## 2. Перенесено в цій сесії

### Поле вводу + вікно чату

| Що | Звідки | Стан |
|---|---|---|
| Розмітка бару | `index.html:696–720` | ✅ дослівно |
| CSS бару, вікна, ручки, повідомлень | `style.css:929–1112` | ✅ дослівно |
| Три крапки | `style.css:1196–1213` | ✅ |
| Бульбашки | `style.css:1480–1482` + `inbox.js:98–102` | ✅ |
| Чіпи в чаті | `style.css:1658–1690` | ✅ |
| Три стани, висоти A/B, увесь жест | `inbox-board.js:25–260` | ✅ |
| Відкриття/закриття, історія, розділювачі | `core.js:894–975`, `800–870` | ✅ |
| `autoResizeTextarea`, `updateChatWindowHeight` | `utils.js:5–39` | ✅ |
| Бейдж непрочитаних | `unread-badge.js` | ✅ |

**Наш файл:** `src/crow/chat.ts`, стилі в `style.css` (блок «Crow dock»).

**Перевірені числа після переносу:** `position:fixed`, 4px від країв,
`bottom = --tabbar-h + 4`, радіус `18 18 10 10`, фон `rgba(38,42,47,0.72)`,
`blur(16px)`, A = 240px (кеп 320), B = 455px, смужка `28×4` над полем тільки
коли чат закритий, зона ручки ~40px, `translateY(110%)` на закритті.

### Таббар (барабан)

| Що | Звідки | Стан |
|---|---|---|
| Розмітка: капсула + трек + кнопка | `index.html:816–825` | ✅ дослівно |
| CSS капсули, клітинок, підписів, кнопки | `style.css:330–462` | ✅ дослівно |
| Фізика: інерція, тертя, snap, межі, дуга | `nav.js:472–663` | ✅ |

**Наш файл:** `src/nav/drum.ts`.

**Перевірені числа:** бар `padding 6px 12px`, `gap 8`, `blur 28`; капсула
`radius 20`, `padding 4`, рамка `1.5px rgba(255,255,255,0.45)`, чотири
внутрішні тіні; трек `gap 2px`, `transition 0.35s cubic-bezier(0.25,0.46,0.45,0.94)`;
`padding-left = half(capsule)` — **повна** половина, як у донора, центрування
робить `snapXFor`; активна клітинка рівно по центру (offset 0); класи
`active / near / far / ""` з opacity підписів `1 / 0.7 / 0.45 / 0`.

---

## 3. Свідомі відступи від донора (і чому)

| Відступ | Причина |
|---|---|
| Палітра: `rgba(15,10,22,x)` → `rgba(38,42,47,x)`, `#1e1040` → `#202326` | Graphite Amber, STAGE-0. Фіолетового немає |
| Один бар замість семи | Рішення Романа: один глобальний чат, `HANDOFF.md §0.5` |
| Кнопка модулів замість «+» | Та сама геометрія `drum-plus-btn` 38×38, інша іконка |
| Рядок UI-контексту у вікні чату | Цього в NeverMind немає — вимога Roma OS |

**Кнопки 32px і 38px, а не 44px.** Це донорські значення. Тест
`every tappable target is at least 44px` виносить їх окремим винятком із
поясненням у коді, а не тихо послаблює поріг. Якщо Роман скаже, що пальцю
незручно — міняти в один бік разом із донором, не поодинці.

---

## 4. Що виглядає дивно і чіпати не можна

- **`.ai-bar-messages` без `mask-image`** — разом із `backdrop-filter` батька
  воно ріже останній елемент на iOS (інцидент UvEHE 03.05).
- **`translateZ(0)` на `.chat-chips-row`** — той самий квірк, чіпи інакше
  обрізає знизу.
- **Зона ручки 40px** при смужці 4px — B-89, палець інакше не влучає.
- **`touch-action: none` на `#tab-bar`** — інакше iOS перехоплює свайп барабана.
- **`.drum-capsule.drum-dragging .tab-item { transition: opacity 0.1s }`** —
  без цього барабан лагає під пальцем.
- **Скидати `transition = 'none'` ДО `setTX(0)`** у `applyPadding` — інакше
  `getBoundingClientRect` читає позицію посеред анімації і барабан стає повз центр.

---

## 5. Що ще НЕ перенесено (черга на наступну сесію)

| Компонент | Де в NeverMind | Примітка |
|---|---|---|
| **Шторка вибору вкладок** | `nav.js:200–413` `openTabSelector` | Зараз наша `module-sheet.ts` — власна. Треба звірити з донором |
| **Модалки** | `modal-overlay-sync.js` + `DESIGN_SYSTEM` «Bottom Sheet» | Наш `ui/sheet.ts` написаний по спеці, але не перенесений з коду |
| **Табло агента** | `owl/board.js` | Наш `crow/board.ts` — поведінка з донора, геометрія своя (так і має бути, §3.5) |
| **Чіпи** | `owl/chips.js` | Перенесено частково: фільтр nav, lock від подвійного тапу. Стрілки прокрутки — свої |
| **`attachSwipeDelete`** | `ui/swipe-delete.js` | Не переносили: в Етапі 1 немає списків з видаленням |
| **Голос** | `ui/voice-input.js` | Перенесено логіку; кнопка тепер у барі донорського вигляду |
| **Фото** | `ui/chat-image.js` | Кнопка на місці, vision іде через Hermes — його немає |

---

## 6. Як переносити наступний компонент

1. Знайти файл і рядки в NeverMind (`grep -n` по класу або функції).
2. Прочитати **весь** блок: CSS + JS + розмітку + коментарі. Коментарі там —
   це історія багів, не прикраса.
3. Скопіювати як є. Замінити тільки палітру та іконки.
4. Виміряти в браузері на 390px і звірити числа з донорськими —
   не «на око», а `getComputedStyle` / `getBoundingClientRect`.
5. Записати сюди рядок у таблицю §2.
