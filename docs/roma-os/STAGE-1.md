# Roma OS — стан Етапу 1

> Вхідний документ лишається `HANDOFF.md`. Цей файл — що зроблено, що лишилось
> заглушкою і що треба перевірити на живому телефоні.
>
> Поточна гілка, HEAD і blockers — у `HANDOFF.md` → ПОТОЧНИЙ СТАН.
> Етап 1 зібрано в гілці `claude/gallant-ride-l1zixz`, яка вже в `main`;
> з `8db3760` це те, що віддає живий URL.

---

## 1. Що є

Фундамент зібраний і працює на розмірі iPhone. Застосунок — статичний PWA без
бекенда: ванільний TypeScript, esbuild, один `index.html`, один service worker.

| Блок | Стан |
|---|---|
| A. Основа | Зліпок старої Control Panel у `legacy/control-panel-v1`. Новий фундамент на геометрії NeverMind. TypeScript у тій самій збірці |
| B. Вигляд | Токени Graphite Amber, примітиви (картка, кнопка, чіп, поле, порожній стан, прогрес, бейдж) |
| C. Дані | Конверт сутності (UUID v7, created/updated, мʼяке видалення, поле під порядок змін), доменні сховища з єдиним контрактом, типізовані адаптери, маркування mock/live на екрані |
| D. Crow | Персонаж із позами, зона з бульбашкою і чіпами, згортання свайпом, **один глобальний чат** із трьома станами, UI context envelope, клавіатура, голос українською |
| E. Навігація | Реєстр модулів, барабан із фізикою NeverMind, шторка модулів із порядком, «Налаштування» карткою-модалкою за шестернею в шапці |
| F. Екрани | Control (Потребує мене / У роботі / Стан системи / Карта системи), Агенти, Проєкти з блокерами, Памʼять, Події |
| G. Перевірка | 22 юніт-тести + 21 тест gateway, 57 e2e на viewport iPhone, lint, типи, сторож кордону, збірка — усе зелене |

## 2. Головне архітектурне рішення етапу

**Один чат Crow + UI context envelope.** Не вісім чатів як у NeverMind.

Перед кожним повідомленням застосунок збирає структурований конверт: активний
модуль, екран, сутність, вибраний елемент, проєкт, агент, фільтри, видимий стан,
блокери, виділення. Конверт видно рядком над повідомленнями — його можна
перевірити очима, а не вірити на слово.

Контрольний випадок працює: Проєкти → тап по блокеру → «А чого це заблоковано?»
→ Crow відповідає саме про цей блокер. Перехід між вкладками не починає нову
розмову і не стирає набране.

## 3. Що перенесено з NeverMind (і що змінено)

| Механіка | Звідки | Чи змінено |
|---|---|---|
| Геометрія екрана | `style.css`, `.page`, `#tab-bar`, `--tabbar-h` | без змін |
| Барабан: інерція, тертя 0.88, snap, гумові краї, дуга (радіус 190, perspective 500) | `src/core/nav.js` | без змін; додано `ResizeObserver` (див. §5) |
| **Поле вводу і вікно чату цілком** — розмітка, CSS, три стани, жест | `index.html:696-720`, `style.css:929-1112`, `inbox-board.js:25-260`, `core.js:894-975`, `utils.js:5-39` | **скопійовано**, не переписано. Свій варіант викинуто. Змінено тільки палітру: `rgba(15,10,22,x)` → `rgba(38,42,47,x)`, `#1e1040` → `#202326`. Один бар замість семи |
| Свайп угору від поля → чат без клавіатури | там же | без змін |
| Підйом під клавіатуру, поріг 250px, повтори після фону | `src/ui/keyboard.js` | без змін |
| Голос `uk-UA`, живий текст, тиша 1200мс, кнопки немає без API | `src/ui/voice-input.js` | без змін |
| Табло: згортання свайпом, пріоритети, стрічка чіпів зі стрілками, fade тексту, вік повідомлення | `src/owl/board.js` | пріоритет — край бульбашки, не заливка (див. §5) |
| Чіпи: фільтр nav→поточна вкладка, lock від подвійного тапу, label = слова Романа | `src/owl/chips.js` | без змін |
| Модалки: overlay як сусід, три способи закриття, картка за пальцем, поріг 80px | `src/ui/modal-overlay-sync.js` + `DESIGN_SYSTEM` | без змін |
| Налаштування: картка-модалка `scale(0→1)`, групи, рядки, тумблер, свайп із ручки | `index.html:1256+`, `style.css:469–571`, `nav.js:734–840` | палітра; лише корисні рядки (Роман, 21.09); тумблер бурштиновий. Деталі → `PORTING.md §2` |
| Делегування `data-action`, жести `data-swipe-detect` | `src/core/delegation.js`, `src/ui/touch-detect.js` | без змін |
| Конверт сутності `stampEntity` | `src/core/entity.js` | без змін |
| Єдиний кордон до моделі + сторож у CI | `openaiFetch` + `check-llm-boundary.js` | те саме, але до Hermes |
| Захист від сирого JSON | `safeAgentReply` | без змін |
| Один шар скла на стек, безпечні зони, глобальна анімація натискання | `DESIGN_SYSTEM` | без змін |

**Геометрія Crow — власна, донора немає.** Повний зріст, зліва, 172px,
поворот 14°, бульбашка справа, чіпи під нею.

**Свідомо НЕ перенесено:** `attachSwipeDelete` — в Етапі 1 немає жодного списку,
з якого можна щось видаляти. Додати разом із першим таким списком, не раніше.

## 4. Що лишилось заглушкою

- **Hermes.** У «Наживо» Crow іде через Roma gateway до справжнього Hermes на
  Mac (`docs/roma-os/GATEWAY.md §4`): одна сесія Hermes на весь чат, одна чиста
  відповідь без reasoning і tool-подій; chips поки порожні, priority `normal`.
  У «Демо» лишається заглушка (`src/hermes/stub.ts`), яка відповідає з локальних
  даних і чесно каже, що вона заглушка. Перемикає `applyDataSource()` разом з
  адаптером даних — мовчазного переходу з одного на інше немає.
- **Дані.** Памʼять може йти наживо з GBrain через Roma gateway
  (`docs/roma-os/GATEWAY.md`), коли в налаштуваннях обрано «Наживо» і задано
  адресу; решта пʼять читань — фікстури (`src/data/seed.ts`) з позначкою
  «демо». Без gateway — усе фікстури.
- **Кнопки шапки** працюють: шестерня → налаштування, дзвіночок → Події,
  «+» → чат.
- **GBrain** підключається лише через gateway, read-only, поза інтернетом;
  на телефоні — тільки нормалізований знімок Памʼяті.

## 5. Знайдено і виправлено під час перевірки на viewport iPhone

1. Барабан ставав на 11px повз центр: padding рахувався проти ширини капсули,
   яка ще не усталилась. Тепер `ResizeObserver` перераховує і центрує.
2. Чіпи (35px) і кнопки мікрофона й відправки (40px) були менші за 44px.
3. Білий текст на бурштині — 2.81:1, нижче AA. Текст на бурштинових поверхнях
   тепер темний (5.15:1). Палітра не змінена.
4. Українські множини ламались після чотирьох («5 блокери») — є `plural()`.
5. Навігація з вибором сутності: перемикання модуля стирало вибір, який щойно
   поставили. Порядок виправлено.
6. Заголовок і підпис у рядку картки зливались в один абзац.
7. Бейдж непрочитаних показував «0» попри `hidden`.
8. `deploy.yml` спрацьовував на цю гілку — перший push перезаписав би живий
   сайт. Тепер деплой тільки з `main`, а перевірки — в окремому `ci.yml`.

## 5.1. Перемикання живого URL зі старої Control Panel

Стара версія ставила service worker через `registerType: 'prompt'` — такий
worker **не** поступається місцем сам. Без окремого заходу телефон, на якому
колись відкривали стару Control Panel, і далі показував би її з кешу, скільки
б разів не відкривали посилання, а новий worker вічно чекав би у «waiting».

Тому новий worker бере керування одразу при install, а при активації
перевіряє: якщо серед видалених кешів був чужий (не `roma-os-*`) — значить
замінили інший застосунок, і сторінки перезавантажуються один раз, щоб
перемикання завершилось без ручного оновлення.

Самообмежене: з наступної версії видаляються тільки власні старі кеші, тому
звичайне оновлення нікого не перезавантажує.

Перевірено локально, обидва сценарії:
- чужий (Workbox) кеш → worker перебирає керування, чужий кеш видалено,
  сторінку перезавантажено один раз, застосунок малюється;
- звичайне оновлення `roma-os` → кеш замінено, перезавантаження немає.

## 5.2. Поле вводу і вікно чату — перенесено дослівно

Перша версія була написана з нуля і виглядала спрощено. Замінено на
справжній бар NeverMind: розмітка, усі значення CSS, уся механіка жесту.

Що збіглося після переносу (перевірено у браузері): `position:fixed`,
відступи 4px від країв, `bottom = --tabbar-h + 4`, радіус `18 18 10 10`,
фон `rgba(38,42,47,0.72)`, `blur(16px)`, висота A 234px (кеп 320),
висота B 449px, смужка-підказка `28×4` над полем тільки коли чат закритий,
зона ручки ~40px, `translateY(110%)` на закритті.

Кнопки фото/мікрофон/відправка — **32px**, як у донорі, а не 44px.
Це свідомо: бар перенесено, а не перероблено. Вони стоять у боксі 50px
з відступами 8px, тож реальна зона пальця більша за саме коло.
Тест `every tappable target is at least 44px` виносить їх окремим
винятком із поясненням, а не мовчки послаблює поріг.

Фото поки не підключене — кнопка на місці, але vision іде через Hermes,
якого немає. Тап показує це словами.

## 5.3. Таббар — перенесено дослівно

Те саме, що з баром: перша версія була своя. Замінено на справжній барабан
NeverMind — розмітка (`index.html:816-825`), уся CSS (`style.css:330-462`),
фізика (`nav.js:472-663`).

Збіглося після переносу: бар `padding 6px 12px`, `gap 8`, `blur 28`;
капсула `radius 20`, `padding 4`, рамка `1.5px`, чотири внутрішні тіні,
бічні градієнти 36px; трек `gap 2px`, `transition 0.35s`;
`padding-left = half(capsule)`; активна клітинка рівно по центру;
класи `active/near/far` з opacity підписів `1 / 0.7 / 0.45 / 0`.

Кнопка модулів — донорська геометрія `drum-plus-btn` 38×38 з пунктирною
рамкою, всередині іконка-куб Roma OS.

**Зауваження:** у NeverMind бар має насичений колір вкладки, і втоплений
жолоб капсули на ньому добре читається. У Roma OS бар світлий
(`rgba(246,240,230,0.92)`), тож жолоб помітно слабший. Порт дослівний;
якщо на телефоні виглядатиме зам'ято — це один рядок (темніша заливка
капсули), але робити без твого слова не буду.

## 6. Що перевірити на реальному iPhone

Headless цього не ловить:

- [ ] Підйом поля під клавіатуру; чи не плутається Safari, що ховає тулбар
- [ ] Свайпи: згортання Crow, три стани чату, закриття шторки
- [ ] Барабан: інерція, snap у центр, гумові межі, підписи що згасають
- [ ] Чи читається втоплений жолоб капсули на світлому барі
- [ ] Rubber-band на краях екрана і всередині чату
- [ ] Матове скло: чи не мерехтить панель і чат при інерційному скролі
- [ ] Безпечні зони: вухо і риска на iPhone 14/15
- [ ] Голосовий ввід українською (у headless Speech API немає)
- [ ] PWA «на головний екран»: іконка, запуск, офлайн
- [ ] Якщо на телефоні колись була стара Control Panel — чи перемкнулось само
      (має перезавантажитись один раз і показати Roma OS)
- [ ] Чи не завеликий Crow на малому екрані (SE)

### 6.1. Шторка модулів — після переносу з `openTabSelector` (сесія 20.09)

Живий URL віддає `main`; шторка з донорськими числами живе на гілці
`claude/funny-shannon-3z3s9q`, поки її не змерджено. Відкривається тапом по
кубу в барабані.

- [ ] Виїжджає знизу за ~0.35 с; фон темніє й розмивається. Коли тягнеш
      картку пальцем, скло **не** «стискається» з країв (iOS-баг UvEHE, який
      лікує overlay-сусід)
- [ ] Ручка стоїть над словом «Модулі», лівіше центру, «Готово» справа в шапці —
      донорська розмітка. Чи не ріже око
- [ ] Тап «Памʼять» → картка бурштинова, галочка; у «Порядок» стає **між**
      Проєкти й Події, не в кінець. Повторний тап вимикає
- [ ] Control: бейдж ЗАВЖДИ, тап нічого не робить
- [ ] Тап чіпа «Агенти» → з'являються ‹ ›; ‹ сірий (перед Control не можна);
      › міняє місцями з Проєкти; повторний тап по чіпу ховає стрілки
- [ ] Стрічка «Порядок» гортається пальцем вбік, і шторка при цьому
      **не** їде вниз (guard `dy > dx`)
- [ ] ‹ › 26px і «Готово» 35px — чи влучає палець. Донорські розміри;
      міняти тільки разом із донором, як 32/38 у барі
- [ ] «Готово» → шторка зникає миттєво, барабан перебудований; після
      перезавантаження набір і порядок ті самі
- [ ] Закриття: тап по темному фону — миттєво; свайп по картці вниз понад
      ~80px — від'їжджає за 0.3 с; менше — пружинить назад; свайп по темному
      фону над шторкою — теж закриває
- [ ] Низ шторки над рискою (safe area + 20px); нічого не обрізано; тіло
      шторки не скролиться і не гумується (5 модулів = 547px, ліміт 85vh)
- [ ] Стоячи на модулі, вимкнути його в шторці → опиняєшся на Control

### 6.2. Після правок 20.09 (ISS-002..007, табло, Control)

- [x] Поле вводу цілком над барабаном із зазором 4px — скріншот «Події» 20.09 23:10; механіка одна на всі екрани
- [ ] Прокрутити стрічку «Події» до кінця: під останнім записом ~20–60px, не порожній екран
- [ ] Прокрутити «Проєкти», зайти в «Події», повернутись — позиція та сама
- [x] Табло: фігура заввишки з бульбашку; чіпи під фігурою і бульбашкою;
      «Поговорити» останній — той самий скріншот
- [ ] Табло: фігура нерухома на всіх вкладках (без погойдування і зміни пози);
      тінь під ногами читається; «Поговорити» відкриває чат
- [ ] Табло однакового розміру на всіх вкладках: фігура, бульбашка і плитки
      під ними не стрибають при перемиканні
- [ ] Бейдж у шапці показує номер деплою і росте після кожного деплою; тап
      відкриває «Інфо про деплой»; «Оновити застосунок» вантажить свіжу збірку
- [ ] Свайп угору по табло: зона їде за пальцем; відпустити раніше 40px —
      повертається; далі — защіпується в смужку, чіпи лишаються
- [ ] Свайп униз по смужці — табло повертається; свайп униз по розгорнутому
      табло — відкривається чат; тап по смужці — теж повертається
- [ ] Чіпи 31px: чи влучає палець (донорський розмір)
- [ ] Метрики: чотири плитки в ряд, усі видно; тап по кожній відкриває
      модуль (Потребує мене / Проєкти → Проєкти, У роботі → Агенти, Сьогодні → Події)
- [ ] Шапка: «+» відкриває чат із курсором у полі і підставляє «Додай »;
      дзвіночок веде в «Події» і гасить крапку (лупу замінила шестерня — §6.3)
- [ ] «Стан системи»: рядок Hermes питає Crow, Агенти й події ведуть у модулі
- [ ] Карта системи: павутина з 5 вузлами навколо ядра, числа живі, тап по
      вузлу відкриває модуль, тап по ядру питає Crow; червона лінія до «Доступи»

### 6.3. Налаштування — картка-модалка за донором (сесія 21.09)

Headless бачить геометрію і логіку; пружину `scale`, скло і скрол під
пальцем — тільки телефон.

- [ ] Шестерня в шапці відкриває картку знизу: вискакує з пружиною
      (`scale 0→1`, 0.35 с), 16px від країв і від низу, матове скло, під ним
      затемнення з блюром
- [ ] Рядок «Roma OS · vN» під заголовком збігається з бейджем у шапці
- [ ] Список скролиться всередині картки без гумування і без «стискання»
      панелі (три групи не влазять у 85vh)
- [ ] Свайп униз по ручці або заголовку понад 80px — картка від'їжджає і
      зникає; менше — пружинить назад; потягнути список — не закриває
- [ ] Тап по темному фону — картка стискається в точку за 0.35 с
- [ ] «Модулі в барабані» → картка закривається, відкривається шторка модулів
- [ ] «Голосовий ввід»: тумблер бурштиновий; вимкнути → мікрофон зникає з поля,
      після перезапуску лишається вимкненим; увімкнути → повертається
- [ ] «Очистити історію чату»: підзаголовок каже кількість; після тапу —
      «Порожньо», у чаті лише привітання, тост
- [ ] «Скинути демо-дані» повертає стартові агенти/проєкти/події, тост
- [ ] «Інфо про деплой» → картка закривається, відкривається шторка з версією
- [ ] «Оновити застосунок» перезавантажує свіжу збірку
- [ ] Картки на екрані з'являються з підйомом на 12px за 0.35 с при кожному
      перемиканні модуля (2–4-та дитина з кроком 50 мс); без ривків поверх
      ковзання екрана

### 6.4. Памʼять наживо через gateway (зріз 1, сесія 21.09)

Потрібні: gateway запущений на Mac, Tailscale Serve, окремий read-токен GBrain
(`GATEWAY.md §7–9`). Headless перевірив усе, крім самої мережі.

- [ ] Налаштування → Джерело даних → «Наживо» → поле адреси зʼявляється;
      адреса без `https://` відхиляється тостом; правильна — «Адресу задано»
- [ ] Памʼять: факти з GBrain, мітка «наживо», «Джерело: gbrain:recall ·
      оновлено щойно»; Control/Проєкти/Події — «демо»
- [ ] Вимкнути Tailscale або приспати Mac → факти лишаються, «Оновлено N хв
      тому · Mac недоступний»; після повернення VPN — оновлюється само
      (повернення у видимість або 90 с)
- [ ] «Демо» назад → знімок зник, Памʼять локальна, факти GBrain не видно

### 6.5. Crow наживо через Hermes (зріз 2 Crow, сесія 21.09 · dmm98t)

Потрібні: gateway із заповненим `HERMES_TOKEN_FILE`, Hermes у server mode на
`127.0.0.1:9119`, Tailscale Serve (`GATEWAY.md §8–10`). Локально доведено
фейковим Hermes у тестах; справжній ланцюг DeepSeek/GBrain — лише на Mac.

- [ ] Налаштування → «Наживо» + адреса → на Control рядок «Hermes» каже
      «наживо», не «заглушка»
- [ ] Написати Crow будь-що → одна відповідь справжнього Hermes; без
      reasoning, без повторів, без службового блоку `[roma-os context]`
- [ ] Спитати про маркер у GBrain → відповідь містить `VIOLET-624`
- [ ] Відкрити блокер і спитати «А чого це заблоковано?» → відповідь про саме
      цей блокер (envelope дійшов)
- [ ] Зупинити Hermes → «Hermes не відповідає.», не відповідь заглушки;
      запустити знову → наступне повідомлення працює без перезапуску gateway
      (session.resume)
- [ ] Перезапустити gateway → розмова триває в тій самій сесії Hermes
      (`HERMES_SESSION_STATE_FILE`), історія з телефона вдруге не сідиться
- [ ] Памʼять наживо далі оновлюється, поки Crow думає
- [ ] Довга відповідь з інструментами (30–60 с) доходить, Serve її не обриває

## 7. Обмеження середовища

- WebKit не завантажується у cloud-сесії. Локально e2e — Chromium з viewport
  iPhone; WebKit вмикається в CI через `PW_WEBKIT=1`.
- `owls68.github.io` заблокований — smoke-тест проти живого URL тільки в CI.
- Pages деплоїться тільки з `main`.

## 8. Відкриті питання до Романа

1. Чи лишається окрема вкладка Notes
2. Шрифтова пара — Bricolage Grotesque + Manrope: підтвердити або замінити.
   Зараз вантажаться з Google Fonts; для офлайну треба покласти локально
3. Коли підключати Productivity
4. Реальний спосіб достукатись до Hermes з телефона
5. Чи потрібна темна тема
6. Кнопки шапки (пошук / сповіщення / «+») — що вони мають робити
